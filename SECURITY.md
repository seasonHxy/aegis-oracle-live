# 安全与产品边界

这是用于本地演示、工程验证和公共测试网试点的单签名者 MVP，未进行独立安全审计。

## 信任假设

- 授权签名者可报告任意符合格式的价格。合约验签不验证 REST 数据，不证明源独立性，也不证明 `confidenceBps` 真实。
- 管理员可改变签名者、feed 参数与暂停状态。所有权转移为两步；MVP 未内置治理延迟或多签。
- 聚合、MAD 和 quorum 不能抵抗多数相关源操纵；IQR 为零也不表示无风险。
- USD/USDT 等计价单位必须由接入方核实。TLS 不代表数据本身正确。

## 已有保护

链与合约域隔离、严格报告序号、时间不倒退、有效期、低 s ECDSA 验签、禁用/暂停 checked read、正价格、固定精度、权限校验、来源超时、响应大小限制、状态文件锁、交易前写入 durable journal。

## 已知范围限制

- 仅支持 31337、11155111、84532，不支持主网。Chain ID 并不能证明 RPC 可信；必须使用可信端点。
- 不支持多签名者共识、KMS/HSM、主备选举、自动交易加价、深重组最终性与多 feed 原子发布。
- Rust 已接入三个交易所的公开 USD 成交接口，但没有股票休市语义、商业行情 SLA 或生产供应商授权；通用 REST 示例仍是模板。
- RPC 钱包必须独占；文件锁无法阻止不同状态路径的进程复用同一个钱包。
- 签名者轮换不会抹除尚未过期的已存价格；需要立即禁止读取时先暂停。
- 待处理交易原始字节包含公开签名数据但不含私钥。保持状态目录完整，不要在交易不明时删除 journal。
- 管理员和报价密钥通过环境变量传入；环境密钥仍受本机进程权限影响，公共部署前应采用独立密钥管理。
- 页面抵押试算使用浏览器数值，链上精确结果使用 `CollateralLens.quote`。该合约不收款、不放贷、不清算。

## 故障处理

1. 暂停新增风险操作；对接协议应保留还款等降低风险的操作。
2. 检查源时间戳、来源错误、离散度、价格跳变及链上 receipt。
3. 有 pending journal 时先按原配置恢复或人工核对链上 nonce，不重建钱包状态。
4. 核实真实市场跳变后记录并调整策略，不应自动忽略熔断。

项目不自称经过审计或可直接承载真实资产。
